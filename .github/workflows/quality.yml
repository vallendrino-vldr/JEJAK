name: Quality Gate

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: quality-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 11.16.0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Format
        run: pnpm format:check

      - name: Secret scan
        run: pnpm secret:scan

      - name: Lint
        run: pnpm lint

      - name: Typecheck
        run: pnpm typecheck

      # Hanya gate deterministik yang jalan di sini. Test yang menembak database
      # sungguhan ada di tests/integration dan supabase/tests; keduanya butuh
      # kredensial produksi, dan repositori ini publik — menjalankannya pada
      # setiap pull request dari siapa pun bukan pertukaran yang sehat.
      # Jalankan `pnpm gate:integrasi` sebelum deploy.
      - name: Unit tests
        run: pnpm test

      # Build memerlukan variabel client agar halaman bisa dirender. Nilai di
      # bawah sengaja palsu dan tidak menunjuk ke project mana pun: yang diuji
      # di sini adalah "kodenya bisa dibangun", bukan konfigurasinya. Artefak
      # build ini tidak pernah dideploy — Vercel membangun ulang dengan
      # environment-nya sendiri.
      - name: Production build
        run: pnpm build
        env:
          NEXT_PUBLIC_SUPABASE_URL: https://contoh-ci-bukan-project-nyata.supabase.co
          NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY: sb_publishable_ci_placeholder_bukan_kunci_nyata

      - name: Production dependency audit
        run: pnpm security:audit
